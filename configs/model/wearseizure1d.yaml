# WearSeizure-1D, Table 4 of the Research Decision Memo.
# Stem k7s2 -> B1..B4 multi-scale depthwise-dilated blocks -> Context -> GAP -> FC(2).
# Budgets are enforced by tests/unit/test_models_shapes_and_budget.py.
name: wearseizure1d
in_channels: 1
input_len: 1024          # 4s @ 256Hz
stem_out_channels: 8
stage_out_channels: [16, 24, 32, 48]
context_channels: 64
kernels: [3, 5]
dilations: [1, 2, 4]      # dilation used by stages B2, B3, B4 respectively (k5 branch)
num_classes: 2
param_budget_max: 32000   # hard ceiling before Gate G1, memo 4.4
mac_budget_max: 2000000
param_target: 13810       # Table 4 total
mac_target: 644000
